# Без этого git на Windows перепишет LF в CRLF при первом же касании
# и покажет «изменены все файлы» там, где не изменилось ни строчки.
* text=auto eol=lf

# Двоичные — не трогать вовсе.
*.png binary
*.exe binary
*.ico binary
